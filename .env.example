LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=EMPTY
LLM_MODEL=qwen3.5-coder-32b

# RAG / Knowledge Base
# Point KB_API_URL at the server's KB query endpoint (same host as LLM_BASE_URL).
# If not set, the RAG falls through to gap mode (LLM answers from own knowledge).
KB_API_URL=http://your-server/kb/v1
KB_API_TIMEOUT=10
CRAG_SCORE_THRESHOLD=0.4
REACT_EVIDENCE_ENABLED=false
PATCH_AGENT_AGENTIC=false
EVIDENCE_RECURSION_LIMIT=12
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_PROJECT=jetson-bsp-agent
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
AUTO_PUSH_ENABLED=false
GIT_REMOTE=origin
BSP_BASE_BRANCH=
GITLAB_WEBHOOK_TOKEN=replace-with-webhook-token
GITLAB_TOKEN=replace-with-a-token-that-can-comment
GITLAB_API_URL=https://gitlab.com/api/v4
BSP_REPO_PATH=/path/to/your/bsp-repo
